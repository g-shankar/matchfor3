{
  "name": "mathquest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "first100:images": "node scripts/fetch-first100-images.mjs",
    "first100:images:validate": "node scripts/validate-first100-images.mjs",
    "first100:audio": "node scripts/generate-first100-audio.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test:cloud": "node tests/cloud-integration.mjs"
  },
  "dependencies": {
    "firebase": "^12.19.0",
    "lucide-react": "^0.468.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vite": "^7.1.0"
  }
}
