import React,{useState} from 'react';import {CalendarDays,ShieldCheck} from 'lucide-react';import {validBirthDate} from './child-profile.js';
export function BirthdatePrompt({onSave,onLater}){const[value,setValue]=useState(''),[error,setError]=useState('');const now=new Date(),fmt=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`,max=fmt(now),oldest=new Date(now.getFullYear()-18,now.getMonth(),now.getDate()),min=fmt(oldest);return <div className="birthday-backdrop"><section className="birthday-dialog" role="dialog" aria-modal="true" aria-labelledby="birthday-title"><span className="birthday-icon"><CalendarDays/></span><div className="eyebrow">GROWN-UP SETUP · SHIVANI</div><h2 id="birthday-title">What is Shivani’s date of birth?</h2><p>This helps MathQuest understand her age as the learning experience grows. It does not change her existing progress.</p><label htmlFor="shivani-birthday">Date of birth<input id="shivani-birthday" type="date" min={min} max={max} value={value} onChange={e=>{setValue(e.target.value);setError('')}}/></label>{error&&<p className="birthday-error" role="alert">{error}</p>}<div className="birthday-privacy"><ShieldCheck/><p>Saved with her private browser and Firebase progress backup. It is not included in parent emails or shared with other players.</p></div><div className="birthday-actions"><button className="text-button" onClick={onLater}>Not now</button><button className="primary" disabled={!value} onClick={()=>{if(!validBirthDate(value)){setError('Please choose a valid date of birth.');return}onSave(value)}}>Save birthday</button></div></section></div>}
